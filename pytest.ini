[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    network: hits Coupang domains over the public internet
    slow: high latency / rate-limit dependent scenario
    ui: Playwright-based browser tests
